---
const navLinks = [
  { url: "/", title: "Home" },
  { url: "/posts/", title: "Posts" },
];
---

<nav role="navigation">
  <ul>
    {
      navLinks.map(({ url, title }) => (
        <li>
          <a href={url}>{title}</a>
        </li>
      ))
    }
  </ul>
</nav>
